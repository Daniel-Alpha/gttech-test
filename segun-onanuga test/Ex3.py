#A function that gives back the first and last character of what was given
def first_last_char(name_2):
    first_char = name_2[0]
    last_char = name_2[-1]
    new_name = first_char + last_char
    return print(new_name)
first_last_char("Segun")